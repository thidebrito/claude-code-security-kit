# Template de variáveis de ambiente — nunca commitar .env real
# Copie pra .env e preencha com os valores reais

# === OpenTimestamps / proteção autoria (subprojeto C) ===
# OTS_CACHE_DIR=~/.ots-cache

# === Webhook de alerta (opcional) ===
# ALERT_WEBHOOK_URL=
